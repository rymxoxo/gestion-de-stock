spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestion_de_stock
    username: postgres
    password: rym
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    database: postgresql
